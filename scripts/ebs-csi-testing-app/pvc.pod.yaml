apiVersion: v1
kind: Pod
metadata:
  name: ebs-app
spec:
  containers:
    - name: ebs-app-container
      image: nginx
      volumeMounts:
      - mountPath: "/var/www/html"
        name: mypd
  volumes:
    - name: mypd
      persistentVolumeClaim:
        claimName: myclaim